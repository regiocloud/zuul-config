---
- project:
    name: regiocloud/zuul-config
    default-branch: main
    merge-mode: "squash-merge"
    check:
      jobs:
        - noop
    gate:
      jobs:
        - noop
    post:
      jobs:
        - noop
    tag:
      jobs:
        - noop
